{
  "name": "@laurenz1606/logger",
  "version": "1.0.0",
  "main": "dist/index.js",
  "bin": "bin/index.js",
  "author": "laurenz1606 <laurenz.rausche@gmail.com>",
  "license": "MIT",
  "scripts": {
    "build": "rimraf ./dist && tsc",
    "prepublish": "yarn build",
    "cleanup": "rimraf ./dist && rimraf ./bin && rimraf yarn.lock && rimraf node_modules && yarn install"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^17.0.5",
    "rimraf": "^3.0.2",
    "typescript": "^4.4.4"
  },
  "files": [
    "dist"
  ]
}
